[project]
name = "chapter01"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mjlee0914", email = "myungjilee0914@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.3.0",
]

[project.scripts]
chapter01 = "chapter01:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
