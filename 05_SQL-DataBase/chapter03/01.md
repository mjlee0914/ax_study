## 서브쿼리 (부속질의)
1. SELECT 문(외부쿼리)에 SELECT(내부쿼리)가 포함된 질의
   - 실행순서: 내부쿼리 -> 외부쿼리
2. 스칼라 서브쿼리
   - 스칼라: 단일값
   - `SELECT "서브쿼리" FROM ~`
3. 인라인 뷰(가상 테이블: 조회결과를 테이블처럼 사용가능한 임시 테이블)
   - `SELECT ... FROM "서브쿼리"`